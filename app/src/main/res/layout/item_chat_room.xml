<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">

    <!-- Avatar placeholder (có thể thêm ImageView cho avatar sau) -->
    <View
        android:id="@+id/avatar_placeholder"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/circle_background"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Receiver Full Name - Tên chính hiển thị trên item_chat_room -->
    <TextView
        android:id="@+id/receiver_full_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:text="Full Name"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#212121"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/avatar_placeholder"
        app:layout_constraintEnd_toStartOf="@id/time_text"
        app:layout_constraintTop_toTopOf="@id/avatar_placeholder" />

    <!-- Receiver Username - Hiển thị bên dưới full name (tùy chọn) -->
    <TextView
        android:id="@+id/receiver_username"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="2dp"
        android:text="@username"
        android:textSize="12sp"
        android:textColor="#757575"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/avatar_placeholder"
        app:layout_constraintEnd_toEndOf="@id/receiver_full_name"
        app:layout_constraintTop_toBottomOf="@id/receiver_full_name" />

    <!-- Last Message -->
    <TextView
        android:id="@+id/last_message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:text="Last message content..."
        android:textSize="14sp"
        android:textColor="#757575"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/avatar_placeholder"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/receiver_username"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Time (optional) -->
    <TextView
        android:id="@+id/time_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="12:30"
        android:textSize="12sp"
        android:textColor="#999999"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/receiver_full_name" />

</androidx.constraintlayout.widget.ConstraintLayout>
